---
RZ\Roadiz\CoreBundle\Entity\Folder:
    iri: Folder
    shortName: Folder
    collectionOperations:
        get:
            method: "GET"
            normalization_context:
                groups:
                    - folder
                enable_max_depth: true
    itemOperations:
        get:
            method: "GET"
            normalization_context:
                groups:
                    - folder
                enable_max_depth: true
